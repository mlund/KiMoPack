name: Tests

on:
  push:
  pull_request:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
        with:
          # setuptools_scm derives the version from tags.
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - run: uv sync --group dev --python ${{ matrix.python-version }}
      - run: uv run --python ${{ matrix.python-version }} ruff check .
      # The bundled datasets are in the checkout, so run the integration tier too:
      # it is what pins the real fitted rates and the correction chain.
      - run: uv run --python ${{ matrix.python-version }} python -m unittest discover -s tests -t . -v
        env:
          KIMOPACK_SLOW_TESTS: "1"
